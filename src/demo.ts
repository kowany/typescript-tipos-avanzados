type UserId = string | number;
let userId: UserId;
userId = 'string';
userId = 1;
console.log('Hello world');
// ts-node
// Permite correr typescript desde el lado de node, nos evita
// estar corriendo el archivo transpilado
